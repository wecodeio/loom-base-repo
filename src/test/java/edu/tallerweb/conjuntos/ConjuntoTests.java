package edu.tallerweb.conjuntos;

import org.junit.Assert;
import org.junit.Test;

public class ConjuntoTests {

	@Test
	public void queSeVerificaEjemplo() {
		/*Conjunto<Integer> conjuntoA = new Conjunto<Integer>();
		conjuntoA.agregar(1);
		conjuntoA.agregar(2);
		conjuntoA.agregar(3);

		Conjunto<Integer> conjuntoB = new Conjunto<Integer>();
		conjuntoA.agregar(3);
		conjuntoB.agregar(4);

		Conjunto<Integer> conjuntoC = conjuntoA.union(conjuntoB);
		
		Assert.assertTrue(
				"que el conjunto contiene el 1",
				conjuntoC.pertenece(1));
		Assert.assertTrue(
				"que el conjunto contiene el 2",
				conjuntoC.pertenece(2));
		Assert.assertTrue(
				"que el conjunto contiene el 3",
				conjuntoC.pertenece(3));
		Assert.assertTrue(
				"que el conjunto contiene el 4",
				conjuntoC.pertenece(4));*/
		Assert.assertTrue(true);

	}
	
}
